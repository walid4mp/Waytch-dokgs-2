# =====================================================================
#  Neon Cipher — PR Labeler
#  Auto-labels pull requests based on changed file paths.
#  Requires .github/labeler.yml (the path -> label mapping).
# =====================================================================
name: PR Labeler

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    name: Apply labels
    runs-on: ubuntu-latest
    steps:
      - name: Label PR
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
